import styled from "styled-components";

const HeaderContainer = () => {
	return (
		<div>
			<div>page list</div>
			<div>toolbar</div>
		</div>
	);
};

export const Header = styled(HeaderContainer)``
